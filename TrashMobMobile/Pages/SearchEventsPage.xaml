<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maps="http://schemas.microsoft.com/dotnet/2021/maui/maps"
             x:Class="TrashMobMobile.Pages.SearchEventsPage"
             Title="Search Events">
    <ScrollView>
        <FlexLayout Direction="Column">

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />

            <Grid RowDefinitions="Auto, Auto, *">
                <HorizontalStackLayout Grid.Row="0">
                    <RadioButton Content="Upcoming" IsChecked="True" RadioButtonGroup.GroupName="EventStatus" CheckedChanged="OnEventStatusRadioButtonCheckedChanged" />
                    <RadioButton Content="Complete" RadioButtonGroup.GroupName="EventStatus" CheckedChanged="OnEventStatusRadioButtonCheckedChanged"/>
                    <RadioButton Content="Both" RadioButtonGroup.GroupName="EventStatus" CheckedChanged="OnEventStatusRadioButtonCheckedChanged"/>
                </HorizontalStackLayout>
                <HorizontalStackLayout Grid.Row="1">
                    <Picker x:Name="CountryPicker" Title="Select a country" ItemsSource="{Binding CountryCollection}" SelectedItem="{Binding SelectedCountry}" />
                    <Picker x:Name="RegionPicker" Title="Select a state/region" ItemsSource="{Binding RegionCollection}" SelectedItem="{Binding SelectedRegion}"/>
                    <Picker x:Name="CityPicker" Title="Select a city" ItemsSource="{Binding CityCollection}" SelectedItem="{Binding SelectedCity}" />
                </HorizontalStackLayout>
                <HorizontalStackLayout Grid.Row="2">
                    <RadioButton Content="Map" IsChecked="True" x:Name="viewMap" FontSize="Micro" />
                    <RadioButton Content="List" x:Name="viewList" FontSize="Micro" />
                </HorizontalStackLayout>
            </Grid>

            <maps:Map x:Name="eventsMap" IsShowingUser="True" ItemsSource="{Binding Events}" HeightRequest="300" IsScrollEnabled="True" IsVisible="{Binding Source={x:Reference viewMap}, Path=IsChecked}" >
                <maps:Map.ItemTemplate>
                    <DataTemplate>
                        <maps:Pin Location="{Binding Address.Location}"
                       Address="{Binding Address.StreetAddress}"
                       Label="{Binding Name}" />
                    </DataTemplate>
                </maps:Map.ItemTemplate>
            </maps:Map>

            <CollectionView x:Name="EventsCollection" 
                 ItemsSource="{Binding Events}" 
                 SelectedItem="{Binding SelectedEvent}"
                 IsVisible="{Binding Source={x:Reference viewList}, Path=IsChecked }"
                 SelectionMode="Single" >
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Margin="5">
                            <Border Padding="3" Stroke="Green">
                                <Grid ColumnDefinitions="1*, 1*"                                  
                                  RowDefinitions="Auto, Auto, Auto, Auto"
                                  x:Name="EventItem">

                                    <Label Text="{Binding Name}" Grid.Row="0" Grid.ColumnSpan="2" FontSize="Small" />
                                    <Label Text="{Binding DisplayDate}" Grid.Row="1" Grid.ColumnSpan="2" FontSize="Micro" />
                                    <Label Text="{Binding DisplayTime}" Grid.Row="1" Grid.Column="1" FontSize="Micro" />
                                    <Label Text="{Binding Address.DisplayAddress}" Grid.Row="2" Grid.ColumnSpan="2" FontSize="Micro" />
                                    <Label Text="{Binding UserRoleForEvent}" Grid.Row="3" FontSize="Micro" />
                                </Grid>
                            </Border>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </FlexLayout>
    </ScrollView>
</ContentPage>
