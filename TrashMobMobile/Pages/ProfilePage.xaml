<?xml version="1.0" encoding="utf-8" ?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:TrashMobMobile.ViewModels"
             x:DataType="viewModels:ProfileViewModel"
             x:Class="TrashMobMobile.Pages.ProfilePage"
             Title="Profile">

    <ScrollView>
        <Grid>
            <VerticalStackLayout Style="{StaticResource OuterVerticalStack}" Spacing="8">

                <!-- ═══ USER HEADER ═══ -->
                <Border Style="{x:StaticResource RoundBorder}" Margin="10,8,10,0">
                    <Grid ColumnDefinitions="Auto, *" ColumnSpacing="14" Padding="14,14">

                        <!-- Avatar circle -->
                        <Border Grid.Column="0"
                                StrokeShape="RoundRectangle 28"
                                StrokeThickness="0"
                                WidthRequest="56" HeightRequest="56"
                                BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                                VerticalOptions="Center">
                            <Label Text="{Binding UserInitials}"
                                   TextColor="White"
                                   FontFamily="LexendSemibold"
                                   FontSize="20"
                                   HorizontalTextAlignment="Center"
                                   VerticalTextAlignment="Center"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center" />
                        </Border>

                        <!-- Name, email, member since -->
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="2">
                            <Label Text="{Binding UserName}"
                                   AutomationId="UserNameLabel"
                                   FontFamily="LexendSemibold"
                                   FontSize="Large" />
                            <Label Text="{Binding UserEmail}"
                                   AutomationId="UserEmailLabel"
                                   FontSize="Small"
                                   TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                            <Label Text="{Binding MemberSince}"
                                   AutomationId="MemberSinceLabel"
                                   FontSize="Micro"
                                   TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>

                <!-- ═══ MY EVENTS ═══ -->
                <Border Style="{StaticResource RoundBorder}" Margin="10,0">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="My Events" Style="{StaticResource SectionHeader}" Margin="0" />
                        <HorizontalStackLayout Spacing="8">
                            <Button Text="Upcoming"
                                    Command="{Binding ShowUpcomingEventsCommand}"
                                    Style="{StaticResource SecondarySmallButton}">
                                <Button.Triggers>
                                    <DataTrigger TargetType="Button" Binding="{Binding ShowUpcoming}" Value="true">
                                        <Setter Property="Style" Value="{StaticResource PrimarySmallButton}" />
                                    </DataTrigger>
                                    <DataTrigger TargetType="Button" Binding="{Binding ShowUpcoming}" Value="false">
                                        <Setter Property="Style" Value="{StaticResource SecondarySmallButton}" />
                                    </DataTrigger>
                                </Button.Triggers>
                            </Button>
                            <Button Text="Completed"
                                    Command="{Binding ShowCompletedEventsCommand}"
                                    Style="{StaticResource SecondarySmallButton}">
                                <Button.Triggers>
                                    <DataTrigger TargetType="Button" Binding="{Binding ShowCompleted}" Value="true">
                                        <Setter Property="Style" Value="{StaticResource PrimarySmallButton}" />
                                    </DataTrigger>
                                    <DataTrigger TargetType="Button" Binding="{Binding ShowCompleted}" Value="false">
                                        <Setter Property="Style" Value="{StaticResource SecondarySmallButton}" />
                                    </DataTrigger>
                                </Button.Triggers>
                            </Button>
                        </HorizontalStackLayout>

                        <!-- Upcoming Events List -->
                        <VerticalStackLayout IsVisible="{Binding ShowUpcoming}">
                            <Label Text="No upcoming events"
                                   IsVisible="{Binding AreNoUpcomingEventsFound}"
                                   FontSize="Small"
                                   Margin="0,4"
                                   TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                            <CollectionView ItemsSource="{Binding UpcomingEvents}"
                                            SelectionMode="None"
                                            IsVisible="{Binding AreUpcomingEventsFound}"
                                            MaximumHeightRequest="300">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate x:DataType="viewModels:EventViewModel">
                                        <Border Style="{x:StaticResource RoundBorder}" Margin="0,3">
                                            <VerticalStackLayout Padding="10,8" Spacing="2">
                                                <Label Text="{Binding Name}" FontFamily="LexendSemibold" FontSize="Small" />
                                                <Label Text="{Binding DisplayDate}" FontSize="Micro"
                                                       TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                                <Label Text="{Binding Address.DisplayAddress}" FontSize="Micro"
                                                       TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                            </VerticalStackLayout>
                                            <Border.GestureRecognizers>
                                                <TapGestureRecognizer
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:ProfileViewModel}}, Path=ViewEventCommand}"
                                                    CommandParameter="{Binding .}" />
                                            </Border.GestureRecognizers>
                                        </Border>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>

                        <!-- Completed Events List -->
                        <VerticalStackLayout IsVisible="{Binding ShowCompleted}">
                            <Label Text="No completed events"
                                   IsVisible="{Binding AreNoCompletedEventsFound}"
                                   FontSize="Small"
                                   Margin="0,4"
                                   TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                            <CollectionView ItemsSource="{Binding CompletedEvents}"
                                            SelectionMode="None"
                                            IsVisible="{Binding AreCompletedEventsFound}"
                                            MaximumHeightRequest="300">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate x:DataType="viewModels:EventViewModel">
                                        <Border Style="{x:StaticResource RoundBorder}" Margin="0,3">
                                            <VerticalStackLayout Padding="10,8" Spacing="2">
                                                <Label Text="{Binding Name}" FontFamily="LexendSemibold" FontSize="Small" />
                                                <Label Text="{Binding DisplayDate}" FontSize="Micro"
                                                       TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                                <Label Text="{Binding Address.DisplayAddress}" FontSize="Micro"
                                                       TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                            </VerticalStackLayout>
                                            <Border.GestureRecognizers>
                                                <TapGestureRecognizer
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:ProfileViewModel}}, Path=ViewEventCommand}"
                                                    CommandParameter="{Binding .}" />
                                            </Border.GestureRecognizers>
                                        </Border>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>

                <!-- ═══ MY LITTER REPORTS ═══ -->
                <Border Style="{StaticResource RoundBorder}" Margin="10,0">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="My Litter Reports" Style="{StaticResource SectionHeader}" Margin="0" />
                        <Label Text="No litter reports"
                               IsVisible="{Binding AreNoLitterReportsFound}"
                               FontSize="Small"
                               Margin="0,4"
                               TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                        <CollectionView ItemsSource="{Binding LitterReports}"
                                        SelectionMode="None"
                                        IsVisible="{Binding AreLitterReportsFound}"
                                        MaximumHeightRequest="250">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="viewModels:LitterReportViewModel">
                                    <Border Style="{x:StaticResource RoundBorder}" Margin="0,3">
                                        <Grid ColumnDefinitions="*, Auto" Padding="10,8">
                                            <VerticalStackLayout Grid.Column="0" Spacing="2">
                                                <Label Text="{Binding Name}" FontFamily="LexendSemibold" FontSize="Small" />
                                                <Label Text="{Binding LitterReportStatus}" FontSize="Micro"
                                                       TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                            </VerticalStackLayout>
                                            <Border Grid.Column="1"
                                                    StrokeShape="RoundRectangle 6"
                                                    StrokeThickness="0"
                                                    IsVisible="{Binding HasThumbnail}"
                                                    WidthRequest="56"
                                                    HeightRequest="56"
                                                    VerticalOptions="Center">
                                                <Image Source="{Binding ThumbnailUrl}"
                                                       Aspect="AspectFill"
                                                       WidthRequest="56"
                                                       HeightRequest="56" />
                                            </Border>
                                        </Grid>
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:ProfileViewModel}}, Path=ViewLitterReportCommand}"
                                                CommandParameter="{Binding .}" />
                                        </Border.GestureRecognizers>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Border>

                <!-- ═══ MY TEAMS ═══ -->
                <Border Style="{StaticResource RoundBorder}" Margin="10,0">
                    <VerticalStackLayout Spacing="8">
                        <Grid ColumnDefinitions="*, Auto">
                            <Label Text="My Teams" Style="{StaticResource SectionHeader}" Margin="0"
                                   Grid.Column="0" />
                            <Button Text="Browse Teams"
                                    Command="{Binding BrowseTeamsCommand}"
                                    Style="{StaticResource SecondarySmallButton}"
                                    Grid.Column="1" />
                        </Grid>
                        <Label Text="You haven't joined any teams yet"
                               IsVisible="{Binding AreNoTeamsFound}"
                               FontSize="Small"
                               Margin="0,4"
                               TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                        <CollectionView ItemsSource="{Binding MyTeams}"
                                        SelectionMode="None"
                                        IsVisible="{Binding AreTeamsFound}"
                                        MaximumHeightRequest="250">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="viewModels:TeamViewModel">
                                    <Border Style="{x:StaticResource RoundBorder}" Margin="0,3">
                                        <VerticalStackLayout Padding="10,8" Spacing="2">
                                            <Label Text="{Binding Name}" FontFamily="LexendSemibold" FontSize="Small" />
                                            <Label Text="{Binding Location}" FontSize="Micro"
                                                   TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                            <Label Text="{Binding MemberCountDisplay}" FontSize="Micro"
                                                   TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                        </VerticalStackLayout>
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:ProfileViewModel}}, Path=ViewTeamCommand}"
                                                CommandParameter="{Binding .}" />
                                        </Border.GestureRecognizers>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Border>

                <!-- ═══ SETTINGS ═══ -->
                <Border Style="{StaticResource RoundBorder}" Margin="10,0">
                    <VerticalStackLayout Spacing="0">
                        <Label Text="Settings" Style="{StaticResource SectionHeader}" Margin="0,0,0,4" />

                        <!-- Location Preference -->
                        <Border Padding="12,12" StrokeThickness="0" StrokeShape="RoundRectangle 0"
                                BackgroundColor="Transparent">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding EditLocationCommand}" />
                            </Border.GestureRecognizers>
                            <Grid ColumnDefinitions="Auto, *, Auto" ColumnSpacing="12">
                                <Image Grid.Column="0" WidthRequest="20" HeightRequest="20" VerticalOptions="Center">
                                    <Image.Source>
                                        <FontImageSource FontFamily="{StaticResource GoogleMaterialIcons}"
                                                         Glyph="{StaticResource IconMapMarkerOutline}"
                                                         Size="20"
                                                         Color="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}" />
                                    </Image.Source>
                                </Image>
                                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                                    <Label Text="Location Preference" FontSize="Small" />
                                    <Label Text="{Binding LocationSummary}" FontSize="Micro"
                                           TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                </VerticalStackLayout>
                                <Image Grid.Column="2" WidthRequest="16" HeightRequest="16" VerticalOptions="Center">
                                    <Image.Source>
                                        <FontImageSource FontFamily="{StaticResource GoogleMaterialIcons}"
                                                         Glyph="{StaticResource IconChevronRight}"
                                                         Size="16"
                                                         Color="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                    </Image.Source>
                                </Image>
                            </Grid>
                        </Border>

                        <BoxView HeightRequest="1" Color="{AppThemeBinding Light={StaticResource BorderLight}, Dark={StaticResource BorderDark}}" Margin="12,0" />

                        <!-- My Dependents -->
                        <Border Padding="12,12" StrokeThickness="0" StrokeShape="RoundRectangle 0"
                                BackgroundColor="Transparent">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding MyDependentsCommand}" />
                            </Border.GestureRecognizers>
                            <Grid ColumnDefinitions="Auto, *, Auto" ColumnSpacing="12">
                                <Image Grid.Column="0" WidthRequest="20" HeightRequest="20" VerticalOptions="Center">
                                    <Image.Source>
                                        <FontImageSource FontFamily="{StaticResource GoogleMaterialIcons}"
                                                         Glyph="{StaticResource IconHumanChild}"
                                                         Size="20"
                                                         Color="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}" />
                                    </Image.Source>
                                </Image>
                                <Label Grid.Column="1" Text="My Dependents" FontSize="Small" VerticalOptions="Center" />
                                <Image Grid.Column="2" WidthRequest="16" HeightRequest="16" VerticalOptions="Center">
                                    <Image.Source>
                                        <FontImageSource FontFamily="{StaticResource GoogleMaterialIcons}"
                                                         Glyph="{StaticResource IconChevronRight}"
                                                         Size="16"
                                                         Color="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                    </Image.Source>
                                </Image>
                            </Grid>
                        </Border>

                        <BoxView HeightRequest="1" Color="{AppThemeBinding Light={StaticResource BorderLight}, Dark={StaticResource BorderDark}}" Margin="12,0" />

                        <!-- Newsletter Preferences -->
                        <Border Padding="12,12" StrokeThickness="0" StrokeShape="RoundRectangle 0"
                                BackgroundColor="Transparent">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding ManageNewsletterPreferencesCommand}" />
                            </Border.GestureRecognizers>
                            <Grid ColumnDefinitions="Auto, *, Auto" ColumnSpacing="12">
                                <Image Grid.Column="0" WidthRequest="20" HeightRequest="20" VerticalOptions="Center">
                                    <Image.Source>
                                        <FontImageSource FontFamily="{StaticResource GoogleMaterialIcons}"
                                                         Glyph="{StaticResource IconEmail}"
                                                         Size="20"
                                                         Color="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}" />
                                    </Image.Source>
                                </Image>
                                <Label Grid.Column="1" Text="Newsletter Preferences" FontSize="Small" VerticalOptions="Center" />
                                <Image Grid.Column="2" WidthRequest="16" HeightRequest="16" VerticalOptions="Center">
                                    <Image.Source>
                                        <FontImageSource FontFamily="{StaticResource GoogleMaterialIcons}"
                                                         Glyph="{StaticResource IconChevronRight}"
                                                         Size="16"
                                                         Color="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                    </Image.Source>
                                </Image>
                            </Grid>
                        </Border>

                        <BoxView HeightRequest="1" Color="{AppThemeBinding Light={StaticResource BorderLight}, Dark={StaticResource BorderDark}}" Margin="12,0" />

                        <!-- Contact Us -->
                        <Border Padding="12,12" StrokeThickness="0" StrokeShape="RoundRectangle 0"
                                BackgroundColor="Transparent">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding ContactUsCommand}" />
                            </Border.GestureRecognizers>
                            <Grid ColumnDefinitions="Auto, *, Auto" ColumnSpacing="12">
                                <Image Grid.Column="0" WidthRequest="20" HeightRequest="20" VerticalOptions="Center">
                                    <Image.Source>
                                        <FontImageSource FontFamily="{StaticResource GoogleMaterialIcons}"
                                                         Glyph="{StaticResource IconEmail}"
                                                         Size="20"
                                                         Color="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}" />
                                    </Image.Source>
                                </Image>
                                <Label Grid.Column="1" Text="Contact Us" FontSize="Small" VerticalOptions="Center" />
                                <Image Grid.Column="2" WidthRequest="16" HeightRequest="16" VerticalOptions="Center">
                                    <Image.Source>
                                        <FontImageSource FontFamily="{StaticResource GoogleMaterialIcons}"
                                                         Glyph="{StaticResource IconChevronRight}"
                                                         Size="16"
                                                         Color="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                    </Image.Source>
                                </Image>
                            </Grid>
                        </Border>
                    </VerticalStackLayout>
                </Border>

                <!-- ═══ LEGAL ═══ -->
                <Border Style="{StaticResource RoundBorder}" Margin="10,0">
                    <VerticalStackLayout Spacing="0">
                        <Border Padding="12,12" StrokeThickness="0" StrokeShape="RoundRectangle 0"
                                BackgroundColor="Transparent">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding OpenPrivacyPolicyCommand}" />
                            </Border.GestureRecognizers>
                            <Grid ColumnDefinitions="Auto, *, Auto" ColumnSpacing="12">
                                <Image Grid.Column="0" WidthRequest="20" HeightRequest="20" VerticalOptions="Center">
                                    <Image.Source>
                                        <FontImageSource FontFamily="{StaticResource GoogleMaterialIcons}"
                                                         Glyph="{StaticResource IconLock}"
                                                         Size="20"
                                                         Color="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                    </Image.Source>
                                </Image>
                                <Label Grid.Column="1" Text="Privacy Policy" FontSize="Small" VerticalOptions="Center"
                                       TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                <Image Grid.Column="2" WidthRequest="16" HeightRequest="16" VerticalOptions="Center">
                                    <Image.Source>
                                        <FontImageSource FontFamily="{StaticResource GoogleMaterialIcons}"
                                                         Glyph="{StaticResource IconChevronRight}"
                                                         Size="16"
                                                         Color="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                    </Image.Source>
                                </Image>
                            </Grid>
                        </Border>

                        <BoxView HeightRequest="1" Color="{AppThemeBinding Light={StaticResource BorderLight}, Dark={StaticResource BorderDark}}" Margin="12,0" />

                        <Border Padding="12,12" StrokeThickness="0" StrokeShape="RoundRectangle 0"
                                BackgroundColor="Transparent">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding OpenTermsOfServiceCommand}" />
                            </Border.GestureRecognizers>
                            <Grid ColumnDefinitions="Auto, *, Auto" ColumnSpacing="12">
                                <Image Grid.Column="0" WidthRequest="20" HeightRequest="20" VerticalOptions="Center">
                                    <Image.Source>
                                        <FontImageSource FontFamily="{StaticResource GoogleMaterialIcons}"
                                                         Glyph="{StaticResource IconFileDocument}"
                                                         Size="20"
                                                         Color="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                    </Image.Source>
                                </Image>
                                <Label Grid.Column="1" Text="Terms of Service" FontSize="Small" VerticalOptions="Center"
                                       TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                <Image Grid.Column="2" WidthRequest="16" HeightRequest="16" VerticalOptions="Center">
                                    <Image.Source>
                                        <FontImageSource FontFamily="{StaticResource GoogleMaterialIcons}"
                                                         Glyph="{StaticResource IconChevronRight}"
                                                         Size="16"
                                                         Color="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />
                                    </Image.Source>
                                </Image>
                            </Grid>
                        </Border>
                    </VerticalStackLayout>
                </Border>

                <!-- ═══ SIGN OUT ═══ -->
                <Button Text="Sign Out"
                        AutomationId="SignOutButton"
                        Command="{Binding SignOutCommand}"
                        Style="{StaticResource PrimaryLargeButton}"
                        Margin="10,4,10,4"
                        ContentLayout="Left, 12"
                        ImageSource="{FontImageSource FontFamily=GoogleMaterialIcons, Glyph={DynamicResource IconLogout}, Color={AppThemeBinding Light={StaticResource PrimaryReverseText}, Dark={StaticResource PrimaryDarkReverseText}}}" />

                <!-- Version -->
                <Label x:Name="VersionLabel"
                       FontSize="Micro"
                       HorizontalTextAlignment="Center"
                       Margin="0,0,0,20"
                       TextColor="{AppThemeBinding Light={StaticResource MutedForegroundLight}, Dark={StaticResource MutedForegroundDark}}" />

            </VerticalStackLayout>
            <BoxView BackgroundColor="{StaticResource TransparentBlack}" HorizontalOptions="Fill"
                     VerticalOptions="Fill" IsVisible="{Binding IsBusy}" />
            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" VerticalOptions="Center"
                               HorizontalOptions="Center" />
        </Grid>
    </ScrollView>
</ContentPage>
