<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maps="http://schemas.microsoft.com/dotnet/2021/maui/maps"
             x:Class="TrashMobMobile.Pages.ViewLitterReportPage"
             Title="Litter Report">
    
    <FlexLayout Direction="Column"
                JustifyContent="SpaceAround">

        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />

            <Grid RowDefinitions="Auto, Auto, Auto, *, Auto">
                <VerticalStackLayout Spacing="5" Grid.Row="0" Margin="10,0">
                    <Label 
                        Text="Name"
                        FontSize="Micro"
                        VerticalOptions="Center" 
                        HorizontalOptions="Start"
                        HorizontalTextAlignment="Center"
                        FontAttributes="Bold" />
                <Frame Style="{x:StaticResource Frame}">
                    <Label 
                        Text="{Binding LitterReportViewModel.Name}"
                        VerticalOptions="Center" 
                        HorizontalOptions="Start"
                        HorizontalTextAlignment="Center"/>
                </Frame>
            </VerticalStackLayout>

            <VerticalStackLayout Spacing="5" Grid.Row="1" Margin="10,0">
                <Label 
                        Text="Description"
                        FontSize="Micro"
                        VerticalOptions="Center" 
                        HorizontalOptions="Start"
                        HorizontalTextAlignment="Center"
                        FontAttributes="Bold" />
                <Frame Style="{x:StaticResource Frame}">
                    <Label 
                        Text="{Binding LitterReportViewModel.Description}"
                        VerticalOptions="Center" 
                        HorizontalOptions="Start"
                        HorizontalTextAlignment="Center"/>
                </Frame>
            </VerticalStackLayout>
            <VerticalStackLayout Spacing="5" Grid.Row="2" Margin="10,0">
                <Label 
                        Text="Status"
                        FontSize="Micro"
                        VerticalOptions="Center" 
                        HorizontalOptions="Start"
                        HorizontalTextAlignment="Center"
                        FontAttributes="Bold" />
                <Frame Style="{x:StaticResource Frame}">
                    <Label 
                        Text="{Binding LitterReportViewModel.LitterReportStatus}"
                        VerticalOptions="Center" 
                        HorizontalOptions="Start"
                        HorizontalTextAlignment="Center"/>
                </Frame>
            </VerticalStackLayout>
                
                <maps:Map x:Name="litterReportLocationMap" ItemsSource="{Binding LitterImageViewModels}" IsShowingUser="True" HeightRequest="300" IsScrollEnabled="True" Grid.Row="3">
                    <maps:Map.ItemTemplate>
                        <DataTemplate>
                            <maps:Pin Location="{Binding Address.Location}"
                              Address="{Binding Address.StreetAddress}"
                              Label="Litter Image" />
                        </DataTemplate>
                    </maps:Map.ItemTemplate>
                </maps:Map>

                <CollectionView x:Name="LitterImagesCollection" Grid.Row="4"
                 ItemsSource="{Binding LitterImageViewModels}" 
                 SelectionMode="None" >
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border StrokeThickness="1">
                                <Grid WidthRequest = "400"
                                  ColumnDefinitions="*, 1*, 1*" 
                                  RowDefinitions="Auto, Auto, Auto, Auto, Auto"
                                  x:Name="LitterImageItem">
                                <Image Source="{Binding AzureBlobUrl}" MaximumWidthRequest="100" Grid.Column="0" Grid.RowSpan="5" />
                                    <Label Grid.Row="0" Grid.Column="1" Text="Address" />
                                    <Label Grid.Row="0" Grid.Column="2" Text="{Binding Address.StreetAddress }" />
                                    <Label Grid.Row="1" Grid.Column="1" Text="City" />
                                    <Label Grid.Row="1" Grid.Column="2" Text="{Binding Address.City }" />
                                    <Label Grid.Row="2" Grid.Column="1" Text="Region" />
                                    <Label Grid.Row="2" Grid.Column="2" Text="{Binding Address.Region }" />
                                    <Label Grid.Row="3" Grid.Column="1" Text="Country" />
                                    <Label Grid.Row="3" Grid.Column="2" Text="{Binding Address.Country }" />
                                    <Label Grid.Row="4" Grid.Column="1" Text="Postal Code" />
                                    <Label Grid.Row="4" Grid.Column="2" Text="{Binding Address.PostalCode }" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

            </Grid>
    </FlexLayout>
</ContentPage>
