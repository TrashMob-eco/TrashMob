assembly-versioning-scheme: MajorMinorPatch
mode: Mainline
next-version: 2.0.0
branches:
  main:
    regex: ^main$
    mode: ContinuousDelivery
    tag: ''
  release:
    regex: ^release$
    mode: ContinuousDelivery
    tag-pre-release-weight: 5000
    tag: ''
ignore:
  sha: []
merge-message-formats: {}
