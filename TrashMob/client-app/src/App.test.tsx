import { render } from '@testing-library/react';
// import { App } from './App';

test.skip('it runs without crashing', () => {
    render(<div />); // Temporary close this test until react is upgrade to 18
    // render(<App />);
});
