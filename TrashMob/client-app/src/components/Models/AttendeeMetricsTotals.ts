export default interface AttendeeMetricsTotals {
    totalBagsCollected: number;
    totalWeightPounds: number;
    totalDurationMinutes: number;
    approvedSubmissions: number;
    pendingSubmissions: number;
    totalSubmissions: number;
}
